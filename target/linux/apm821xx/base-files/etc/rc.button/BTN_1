#!/bin/sh

. /lib/functions.sh
. /lib/apm821xx.sh

BOARD=$(apm821xx_board_name)

if [ $BOARD == "wndr4700" ]; then
	case "$ACTION" in
	released)
		modprobe dwc2
	;;
	pressed)
		rmmod dwc2
	;;
	esac
fi

return 0
